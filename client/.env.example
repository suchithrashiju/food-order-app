# Leave empty in local development to use the Vite /api proxy.
# For production / hosted builds, set the public API origin (no trailing slash).
# Example: https://your-api.onrender.com
VITE_API_URL=

# Optional. Defaults to VITE_API_URL, or http://localhost:3000 in Vite dev.
# VITE_SOCKET_URL=
